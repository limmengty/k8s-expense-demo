---
# ============================================================
# demok8s / apps/expense-ui/base/pdb.yaml
# ============================================================
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: expense-ui-pdb
  namespace: expense-ui
  labels:
    app.kubernetes.io/name: expense-ui
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: expense-ui
