---
# ============================================================
# demok8s / apps/expense-api/base/serviceaccount.yaml
# Principle of least privilege — dedicated SA per app
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: expense-api-sa
  namespace: expense-api
  labels:
    app.kubernetes.io/name: expense-api
automountServiceAccountToken: false
