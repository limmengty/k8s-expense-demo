---
apiVersion: v1
kind: ConfigMap
metadata:
  name: expense-api-configmap
  namespace: expense-api
  labels:
    app.kubernetes.io/name: expense-api
data:
  # Matches application.yml: ${DATABASE_URL}
  DATABASE_URL: "jdbc:postgresql://postgres-service:5432/expense_db"
  # Matches application.yml: ${DATABASE_USERNAME:postgres}
  DATABASE_USERNAME: "postgres"
  # Matches application.yml: ${KEYCLOAK_ISSUER_URI} — used for BOTH jwt.issuer-uri AND keycloak.issuer-uri
  KEYCLOAK_ISSUER_URI: "https://keycloak.limmengty.com/realms/expense"
  # Matches application.yml: ${KEYCLOAK_SERVER_URL}
  KEYCLOAK_SERVER_URL: "https://keycloak.limmengty.com"
  # Matches application.yml: ${KEYCLOAK_REALM:expense}
  KEYCLOAK_REALM: "expense"
  # Matches application.yml: ${KEYCLOAK_CLIENT_ID:expense-api}
  KEYCLOAK_CLIENT_ID: "expense-api"
  JAVA_OPTS: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -Djava.security.egd=file:/dev/./urandom"
