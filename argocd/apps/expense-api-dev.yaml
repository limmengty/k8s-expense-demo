---
# ============================================================
# demok8s / argocd/apps/expense-api-dev.yaml
# ArgoCD Application — expense-api dev environment
# Apply: kubectl apply -f argocd/apps/expense-api-dev.yaml -n argocd
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: expense-api-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: expense-api
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: expense

  source:
    repoURL: https://github.com/limmengty/k8s-expense-demo.git
    targetRevision: main
    path: apps/expense-api/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: expense-api-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas   # HPA manages this
